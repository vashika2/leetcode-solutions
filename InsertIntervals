import java.util.*;

public class InsertIntervals {

    public static void main(String[] args) {
        int[][] intervals = {
            {1,2},{3,5},{6,7},{8,10},{12,16}
        };

        int[] newInterval = {4,8};

        Solution sol = new Solution();
        int[][] result = sol.insert(intervals, newInterval);

        for (int[] i : result) {
            System.out.println(Arrays.toString(i));
        }
    }
}

class Solution{
    public int[][] insert(int[][] intervals, int[] newIntervals){
        List<int[]> result = new ArrayList<>();
        int i =0;
        int n = intervals.length;

        // add the ones that come before and dont overlap
        while( i<n && intervals[i[1]<newIntervals[0]]){
            result.add(intervlas[i]);
            i++;
        }

        // add the ones that overlap
        while(i<n && intervals[i][0]<newIntervals[1]){
            newInterval[0] = Math.min(newInterval[0], intervals[i][0]);
            newInterval[1] = Math.max(intervals[i][1], intervals[i][1]);
            i++; 
        }
        result.add(newIntervals);
    }
    // add the rest after the new interval

    while(i<n){
        result.add(intervals[1]);
        i++;
    }
    return result.toArray(new int[result.size()][]);
}